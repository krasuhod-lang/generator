# Прототип AI-помощника «Выручай-Сенсей»

Интерактивный прототип чат-виджета для сервиса подбора микрозаймов **«Выручай»**, реализующий техническое задание из issue. Прототип — статический сайт (HTML / CSS / vanilla JS), его можно открыть локально без сборки и без бэкенда.

> **Важно:** это front-end-прототип, демонстрирующий **UX/UI** и **сценарии диалога** из ТЗ. NLP/LLM/RAG/брокеридж представлены **mock-реализациями** на клиенте, чтобы можно было «пощупать» поведение Сенсея до подключения реальных сервисов (YandexGPT / GigaChat / OpenAI + pgvector + API-шлюз МФО).

---

## 🚀 Как запустить

Никакой сборки не требуется. Достаточно одного из вариантов:

```bash
# Вариант 1: прямо открыть файл в браузере
open index.html        # macOS
xdg-open index.html    # Linux
start index.html       # Windows

# Вариант 2: поднять локальный статик-сервер
python3 -m http.server 8000
# затем открыть http://localhost:8000
```

---

## 🗂 Структура (v2 — Phase 0 рефакторинг)

```
.
├── index.html              # Лендинг + разметка виджета и чата
├── styles.css              # Все стили (включая v2-карточки и тикер)
├── app.js                  # Bootstrap: главное меню v2, маршрутизация, magic-link
├── core/
│   ├── state.js            # Глобальное состояние + session_id (UUID)
│   ├── util.js             # Форматирование, аннуитет, escape
│   ├── kb.js               # Mock-RAG: знания по всем категориям
│   ├── nlp.js              # Rule-based intent + parseAmount/parseTerm
│   ├── render.js           # Баблы, typing, ачивки, карусель, реестр карточек
│   ├── dialog.js           # Schema-driven движок шагов
│   └── api.js              # getOffers / getMagicLink (mock ⇄ /api/v1/*)
├── categories/
│   ├── loans.js            # 💰 Микрозаймы (Sprint 1, baseline)
│   ├── credits.js          # 🏦 Потребкредиты (§1.2) + калькулятор переплаты
│   ├── mortgages.js        # 🏠 Ипотека (§1.3) + auto-програмы + сравн. таблица
│   ├── cards.js            # 💳 Кредитные карты (§1.4) + годовой кэшбэк
│   ├── insurance.js        # 🛡 ОСАГО/КАСКО/ДМС/... (§1.5) с под-роутером
│   ├── deposits.js         # 📈 Вклады (§1.6) + анимированный «Счётчик дохода»
│   ├── refinancing.js      # 🔄 Рефинансирование (§1.7) + точка безубыточности
│   └── prequalification.js # 🎯 Квиз пред-скоринга (с явным согласием, мок-БКИ)
└── README.md
```

Каждая категория самодостаточна и состоит из:
* `qualification_schema` — декларативный список шагов;
* `buildOffers(params, profile)` — mock-генератор офферов;
* `cards.register(category, fn)` — рендерер карточки.

Чтобы добавить новую категорию — создайте файл в `categories/` и подключите в `index.html`.

---

## 🆕 Что добавлено в v2

* **Главное меню v2** — 9 пунктов из ТЗ §1.8 (loan / credit / mortgage / card / insurance / deposit / refinancing / rehab / faq).
* **6 новых продуктов** (см. таблицу ниже) с полностью рабочими шагами и карточками офферов.
* **Калькулятор переплаты** для потребкредитов (overlay со сравнением банков).
* **Авто-определение льготной/семейной/IT-ипотеки** по ответам пользователя.
* **Сравнительная таблица «Господдержка vs Рыночная»** в карточках ипотеки.
* **Расчёт «Сколько кэшбэка за год»** для кредитных карт.
* **Анимированный «Счётчик дохода»** (rAF) для вкладов.
* **Расчёт точки безубыточности** для рефинансирования с рекомендацией.
* **Session-id (UUID)** в `sessionStorage` — нужен для magic-link, аналитики и серверной геймификации (Phase 5+).
* **Фасад `Sensei.api`** с feature-flag `USE_MOCK` — переключение на реальный `/api/v1/offers` одной строчкой при появлении бэка (Phase 3 плана).

| Категория | Шагов | Партнёры (mock) | Особенности |
|---|---|---|---|
| 💰 Займ        | 5 | 5 МФО                         | Sprint-1 baseline, без изменений UX |
| 🏦 Кредит      | 7 | Сбер / Тинькофф / Альфа / ВТБ / Почта / Хоум | Аннуитет + калькулятор переплаты |
| 🏠 Ипотека     | 7 | ДОМ.РФ / Сбер / ВТБ / Альфа / Росбанк | Авто-програмы, сравн. таблица, дисклеймер |
| 💳 Карта       | 4 | Тинькофф / Альфа / Сбер / РСБ | Годовой кэшбэк, грейс-период |
| 🛡 Страхование | 6 | Сбер / Ингосстрах / Альфа / Тинькофф / РЕСО / СОГАЗ | Под-роутер; полный ОСАГО + универс. для остальных |
| 📈 Вклад       | 4 | Сбер / ВТБ / Тинькофф / Альфа / ГПБ | rAF-тикер дохода, АСВ-бейдж |
| 🔄 Рефинанс    | 5 | Сбер / Тинькофф / Альфа / ВТБ / ГПБ | Точка безубыточности, recommend/warn |
| 🎯 Пред-квалификация | 8 + согласие | МКК «Выручай» (мок) | Прозрачное согласие 152-ФЗ, мок-скоринг low/medium/high, опциональный мок-запрос БКИ (218-ФЗ) |

---

## 🎯 Пред-квалификация (квиз «Узнай шансы за 1 минуту»)

Это отдельная категория (`categories/prequalification.js`), запускаемая
из главного меню пунктом **«🎯 Узнать шансы за 1 минуту»**. Её цель —
быстро оценить потенциал пользователя на одобрение займа.

### Что собираем и зачем

| Поле | Зачем | Источник |
|---|---|---|
| Имя | Обращение в чате | Пользователь вводит сам |
| Телефон | Связь по заявке | Пользователь вводит сам, формат `+7…` |
| Сумма, срок | Подбор офферов | Пользователь вводит сам |
| Цель займа | Уточнение продукта | Quick-replies |
| Доход | Скоринг (диапазон, не точная сумма) | Quick-replies |
| Тип занятости | Скоринг | Quick-replies |
| Просрочки за 12 мес. | Скоринг | Quick-replies |

**На каком основании.** Согласие пользователя (152-ФЗ) — собирается
**первым шагом квиза**, явной кнопкой. Без согласия квиз не запускается.
Текст согласия версионируется (`CONSENT_VERSION` в `core/consent.js`):
при изменении формулировок все ранее данные согласия автоматически
становятся недействительными.

### Что мы НЕ делаем (намеренно)

* ❌ **Не читаем куки сторонних доменов.** Это и невозможно из браузера
  (`document.cookie` видит только свой origin), и было бы прямым
  нарушением 152-ФЗ.
* ❌ **Никакого «скрытого» pre-fill** телефона/имени. Все поля заполняет
  сам пользователь явно.
* ❌ **Не вызываем БКИ из браузера.** Реальный запрос кредитной истории
  возможен только с серверной стороны после отдельного согласия (218-ФЗ).
  В прототипе кнопка «Уточнить по кредитной истории» — это **мок**:
  она вызывает заглушку `Sensei.api.getCreditScore()` и возвращает
  детерминированный псевдо-результат на основе `session_id`.
* ❌ **Не запускаем скоринг до согласия.** Шаг согласия — обязательный
  первый шаг схемы.

### Куки и localStorage

Используются **только свои** ключи с префиксом `sensei_`, **никаких
третьих сторон**:

| Ключ | Назначение | Когда сохраняется |
|---|---|---|
| `sensei_sid` (sessionStorage) | Корреляционный session_id, нужен для magic-link | Всегда (технически необходимо) |
| `sensei_cookie_choice` | Выбор из cookie-баннера: `all` / `essential` | Когда пользователь нажал на одну из кнопок баннера |
| `sensei_consent_pdn` | Согласие на обработку ПДн (152-ФЗ) с версией и timestamp | После явного согласия в квизе |
| `sensei_consent_bki` | Согласие на запрос КИ (218-ФЗ) с версией и timestamp | После явного согласия в БКИ-флоу |
| `sensei_quiz_progress` | Прогресс квиза для восстановления при перезагрузке | Только если есть `sensei_consent_pdn` **и** `cookie_choice === 'all'` |

Cookie-баннер с двумя кнопками (**«Принять всё»** / **«Только
необходимые»**) показывается на лендинге, пока пользователь не сделает
выбор. По умолчанию (баннер не закрыт) прогресс квиза **не сохраняется** —
privacy by default.

### Где это мок

* `categories/prequalification.js → buildOffers()` — мок-скоринг по
  простым правилам (доход vs сумма, занятость, просрочки, срок).
  Возвращает 2–4 варианта (PDL / installment / залоговый / реабилитация).
* `core/api.js → getCreditScore()` — мок-запрос в БКИ. В проде
  заменяется на серверный `POST /api/v1/bki/request` (см. TODO в коде).
* Анкетные данные БКИ-флоу (паспорт, ДР, код субъекта КИ) в прототипе
  **не запрашиваются** через чат и нигде не сохраняются — это
  намеренно. В проде такая форма открывается через Magic-Link в
  защищённом ЛК с паспорт-сканером Smart Engines.

---

## 🛣 Дальнейшие фазы (вне этого PR)

Phases 2–8 плана требуют отдельного back-end-репозитория (gateway / LLM / RAG / Magic-Link / аналитика). Front-end готов к подключению — достаточно установить `Sensei.api.setUseMock(false)` и поднять API-шлюз с эндпоинтами `/api/v1/offers`, `/api/v1/magic-link`, `/api/v1/chat (SSE)`.

---

## ✅ Покрытие пунктов ТЗ

| Пункт ТЗ | Что реализовано в прототипе | Где |
|---|---|---|
| §2.1 Точка входа | Плавающая иконка 🥋 в правом нижнем углу + бейдж-подсказка «Нужны деньги? Помогу подобрать оффер с одобрением 98%!» | `index.html` (`.sensei-launcher`), `app.js` (open/close) |
| §2.1 Telegram-like UI | Скруглённые баблы, корп. цвета (синий/фиолетовый + зелёный/оранжевый акценты), анимация «печатает…», quick replies, карусель | `styles.css` |
| §2.2 Сценарий №1 (подбор займа) | Полный flow: интент → парс суммы/срока → микро-скоринг (просрочки) → возраст → тип занятости → карусель из топ-3 офферов | `app.js`: `advanceLoanFlow`, `showOffers` |
| §2.2 Шкала вероятности | Зелёный/оранжевый/красный прогресс-бар «Шанс одобрения: N%» в каждой карточке | `app.js`: `renderOffers`; `styles.css`: `.bar-fill` |
| §2.2 «Оформить в 1 клик» | Генерация magic-link с токеном и редирект в защищённый ЛК | `app.js`: `handleApply` |
| §2.3 Сценарий №2 (FAQ) | Ответы из базы знаний (mock-RAG): досрочное погашение, ставки, суммы, требования, способы выдачи, КИ, паспорт, оператор | `app.js`: `KNOWLEDGE_BASE`, `findFaq` |
| §2.4 Сценарий №3 (геймификация) | «Программа реабилитации» КИ, уровни (Ученик → Подмастерье → Самурай), ачивки в чате | `app.js`: `startRehab`, `setLevel`, `showAchievement` |
| §3.2 Системный промпт | Соблюдены правила: не обещаем 100% («высокий шанс»), при отсутствии ответа в RAG — оператор, после сбора {сумма, срок, просрочки} — `getLoanOffers()` | `app.js`: `handleUserText`, `showOffers` |
| §3.4 Безопасность | Паспортные данные **не запрашиваются в чате**, дисклеймер в футере чата, выдача magic-link для перехода в защищённый контур (Smart Engines) | `index.html` (`.chat-disclaimer`), `app.js`: `handleApply` |

---

## 🤖 NLP-парсер (mock)

Распознаёт русскоязычные формулировки без LLM, чтобы прототип «понимал» свободный ввод:

| Ввод пользователя | Что распознаётся |
|---|---|
| `Мне нужно 30 тысяч до зарплаты` | сумма = 30 000 ₽, срок = 30 дн., интент = `loan_request` |
| `хочу 10к на неделю` | сумма = 10 000 ₽, срок = 7 дн. |
| `дай десятку` | сумма = 10 000 ₽ |
| `как погасить досрочно?` | интент = `faq` → ответ из базы знаний |
| `позови оператора` | интент = `faq` → перевод |

Парсеры: `parseAmount`, `parseTerm`, `detectIntent` в `app.js`.

В продакшене эта логика заменяется вызовом LLM (YandexGPT / GigaChat / OpenAI) с **системным промптом из §3.2 ТЗ** и function-calling в `get_loan_offers()`.

---

## 🧠 Системный промпт (для интеграции с LLM)

Готовый текст промпта, соответствующий §3.2 ТЗ, должен быть передан в LLM при каждом запросе:

```text
Ты — Сенсей, умный и дружелюбный финансовый помощник сервиса "Выручай".
Задача: помогать пользователям подбирать микрозаймы и консультировать
по финансовым вопросам.

Тон: вежливый, поддерживающий, элементы восточной философии (Дзен,
путь самурая, мудрость), без перебора. Обращение на "Вы".

Правила:
1. Никогда не обещай 100% одобрение. Говори "высокий шанс одобрения".
2. Условия продукта — ТОЛЬКО из базы знаний (RAG). Если ответа нет —
   предлагай перевести на оператора. Галлюцинации недопустимы.
3. После сбора {Сумма, Срок, Наличие текущих просрочек} вызывай
   функцию get_loan_offers(amount, term_days, overdue).
4. Никогда не запрашивай паспортные данные в чате. Для оформления —
   выдавай magic-link в защищённый ЛК.
```

---

## 🛣 Дорожная карта (соответствует §4 ТЗ)

- **Sprint 1 — Chat UI & Core Logic** ✅ *(этот прототип)*
  Виджет, quick replies, сбор {сумма, срок} по жёсткому скрипту.
- **Sprint 2 — Brokerage API** — заменить `getLoanOffers()` в `app.js` на реальный вызов API-шлюза, возвращающего отсортированный JSON-массив офферов МФО.
- **Sprint 3 — AI & RAG** — подключить LLM с системным промптом и pgvector/ChromaDB с распарсенным контентом `viruchay.ru` (статьи о КИ, условия, требования). Заменить `KNOWLEDGE_BASE` и `detectIntent` на retrieval + генерацию.
- **Sprint 4 — Gamification** — расширить уровни (Ученик / Подмастерье / Самурай / Дзен), persist прогресса в БД, добавить аналитику ачивок и push-нотификации о росте лимита.

---

## 🔌 Куда смотреть для интеграции

| Что подменять | Файл / функция | Чем заменить |
|---|---|---|
| Mock-API брокериджа | `app.js` → `getLoanOffers()` | `fetch('/api/v1/offers', { method: 'POST', body: ... })` к API-шлюзу |
| Mock-NLP | `app.js` → `detectIntent`, `parseAmount`, `parseTerm` | Вызов LLM (YandexGPT / GigaChat / OpenAI) с system prompt и tool-calls |
| Mock-RAG | `app.js` → `KNOWLEDGE_BASE`, `findFaq` | Запрос в векторную БД (pgvector / Pinecone / Chroma) + LLM-ответ по retrieval |
| Magic-link | `app.js` → `handleApply` | Серверный эндпоинт `/api/v1/magic-link` с привязкой к сессии и MFO |

---

## 🎨 Дизайн

- **Шрифт:** системный sans-serif (Segoe UI / SF / Roboto) — для нативного ощущения.
- **Палитра:**
  - `--c-primary: #3b2e8c` — глубокий фиолетово-синий (доверие)
  - `--c-primary-2: #5b46d6` — для градиентов
  - `--c-accent: #18b86b` — зелёный (действие, высокий шанс)
  - `--c-accent-warn: #f5a524` — оранжевый (средний шанс / самозанятый)
  - `--c-danger: #e5484d` — красный (низкий шанс / просрочки 90+)
- **Адаптив:** на ширине < 480px чат раскрывается на весь экран.

---

## 🧪 Сценарии для проверки прототипа в браузере

1. **Свободный ввод** — кликнуть «Поговорить с Сенсеем», написать `Мне нужно 30 тысяч до зарплаты`. Бот распознает сумму и срок и сразу спросит про просрочки.
2. **Quick replies** — пройти flow только кнопками: `Нужны деньги` → `30 000 ₽` → `30 дней` → `Нет просрочек` → `26–40` → `Найм` → карусель офферов.
3. **FAQ** — спросить `как погасить досрочно?` или `какая ставка?` — ответ из базы знаний с кнопкой follow-up.
4. **Реабилитация КИ** — нажать `📈 Прокачать КИ` или ответить `Есть, более 90 дней` — Сенсей предложит «Программу реабилитации» вместо отказа.
5. **Magic-link** — нажать «Оформить в 1 клик» в любой карточке оффера — Сенсей сгенерирует ссылку на защищённый ЛК.
6. **Геймификация** — пройти flow до конца: появятся ачивки `🎯 Первая консультация`, `⚔️ Чистая КИ`, бейдж уровня в шапке чата сменится на «Самурай».
